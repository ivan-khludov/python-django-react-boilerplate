# python-django-react-boilerplate

Install dependencies:

```
yarn
```

Generate Django secret key (it only needs to be run once):

```
./bootstrap.sh
```

Start Django server with:

```
yarn dev
```
